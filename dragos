index.html<!DOCTYPE html>
<html lang="ro">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Dragoș 💘</title>

  <style>
    body {
      background: linear-gradient(to bottom, #ffe6f0, #fff);
      font-family: Arial, sans-serif;
      text-align: center;
      padding: 40px;
      overflow: hidden;
    }

    h1 {
      font-size: 2.3rem;
      color: #ff2e74;
    }

    p {
      font-size: 1.2rem;
      color: #444;
    }

    img {
      width: 230px;
      margin-top: 15px;
      border-radius: 20px;
    }

    .buttons {
      margin-top: 35px;
      position: relative;
      height: 200px;
    }

    button {
      font-size: 1.4rem;
      padding: 15px 40px;
      border: none;
      border-radius: 14px;
      cursor: pointer;
      position: absolute;
      transition: 0.2s;
    }

    #yesBtn {
      background: #2ecc71;
      color: white;
      left: 20%;
    }

    #noBtn {
      background: #e74c3c;
      color: white;
      left: 55%;
    }

    #message {
      margin-top: 35px;
      font-size: 2rem;
      color: #ff2e74;
      font-weight: bold;
    }

    /* inimioare care cad */
    .heart {
      position: absolute;
      color: pink;
      font-size: 20px;
      animation: fall 4s linear infinite;
    }

    @keyframes fall {
      from { transform: translateY(-50px); }
      to { transform: translateY(700px); }
    }
  </style>
</head>

<body>

  <h1>Dragoș... 💘</h1>
  <p>Vrei să fii Valentine-ul meu? 🐹💕</p>

  <!-- Capibara GIF -->
  <img src="https://media.tenor.com/1D7zKxQn4VAAAAAC/capybara-love.gif">

  <!-- Muzică romantică -->
  <audio autoplay loop>
    <source src="https://www.bensound.com/bensound-music/bensound-romantic.mp3" type="audio/mp3">
  </audio>

  <div class="buttons">
    <button id="yesBtn">DA 💖</button>
    <button id="noBtn">NU 😭</button>
  </div>

  <div id="message"></div>

  <script>
    const noBtn = document.getElementById("noBtn");
    const yesBtn = document.getElementById("yesBtn");
    const message = document.getElementById("message");

    // Butonul NU fuge
    noBtn.addEventListener("mouseover", () => {
      const x = Math.random() * 250;
      const y = Math.random() * 150;
      noBtn.style.left = x + "px";
      noBtn.style.top = y + "px";
    });

    // Apasă DA
    yesBtn.addEventListener("click", () => {
      message.innerHTML = "YAAAY Dragoș 😭💖 Capibara dansează de fericire!! 🎉🐹";

      // Confetti inimioare
      for (let i = 0; i < 25; i++) {
        createHeart();
      }
    });

    // Funcție inimioare care cad
    function createHeart() {
      const heart = document.createElement("div");
      heart.classList.add("heart");
      heart.innerHTML = "💗";

      heart.style.left = Math.random() * window.innerWidth + "px";
      heart.style.animationDuration = (Math.random() * 2 + 3) + "s";

      document.body.appendChild(heart);

      setTimeout(() => {
        heart.remove();
      }, 4000);
    }
  </script>

</body>
</html>
